# Core
pyyaml>=6.0
pydantic>=2.0

# Browser controller
playwright>=1.44

# Scheduler (Step 6)
apscheduler>=3.10

# Dashboard server
fastapi>=0.111
uvicorn[standard]>=0.29

# Notifications (Step 5)
httpx>=0.27          # Telegram HTTP calls + server health check in main.py

# App window (wraps dashboard in a native frame)
pywebview>=5.0

# Anti-bot / stealth
playwright-stealth>=2.0  # patches browser fingerprint to avoid bot detection

# Audio CAPTCHA solving (offline)
openai-whisper>=20250625  # local speech-to-text for audio CAPTCHA challenges
# Also requires: ffmpeg binary in PATH (winget install Gyan.FFmpeg)

# OCR
Pillow>=10.0          # image loading for both Tesseract and EasyOCR
pytesseract>=0.3.10   # thin wrapper around the Tesseract binary (install separately)
easyocr>=1.7          # GPU-accelerated deep learning OCR (pulls PyTorch)

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
